# Fortress V6 Plan D: 集成测试 + 容器交付

> Final plan — E2E attack scenarios + OCI container + release verification.

**Goal:** End-to-end attack→detect→score→respond tests, Dockerfile, final verification.

**Tasks:** D1 (E2E tests), D2 (Dockerfile + build scripts), D3 (final verification + tag)
